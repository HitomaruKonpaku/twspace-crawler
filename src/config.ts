export const config = {
  app: {
    mediaDir: '../.media',
    userRefreshInterval: 10000,
    dynamicPlaylistRefreshInterval: 5000,
  },
  logger: {
    dir: './logs',
    datePattern: 'YYMMDD',
  },
}
